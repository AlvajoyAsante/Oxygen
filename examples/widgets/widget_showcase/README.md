# Widget showcase

This example demonstrates a small set of Oxygen widgets in a single program:

- A clickable button that updates the status text
- A checkbox that can be toggled on and off
- A switch that can be toggled on and off
- Label widgets for the title and status line

Use the on-screen cursor to interact with the widgets and press Clear to exit.
